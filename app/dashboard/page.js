import React from "react";
import Header from "@/ui/dashboard/header/Header";

export const metadata = {
  title: "Your Dashboard | Limitless Cover",
};

const page = () => {
  return (
    <div>
      <div className={"centeredContent"}></div>
    </div>
  );
};

export default page;
